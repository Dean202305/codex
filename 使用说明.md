# 简历初筛工具使用说明

这份文档给日常使用看，不讲代码，只讲怎么配置、怎么运行、结果怎么看。

## 这个工具做什么

这个工具会自动做这几件事：

1. 递归扫描 `/Users/mac/Downloads` 里面的文件，包括子文件夹。
2. 每次运行都重新读取岗位要求表：
   `/Users/mac/Downloads/小A自动化岗位说明书.xlsx`
3. 识别简历内容，并根据岗位要求调用模型做初步筛选。
4. 把结果追加写入招聘表：
   `/Users/mac/Downloads/小A科技（北京）组织招聘.xlsx`
5. 不覆盖历史记录，只往下新增。
6. 如果发现简历内容重复，会照常新增一行，但整行标红。
7. 如果模型超时、API 报错、正文无法提取、岗位名称匹配不到岗位说明书，会标记为 `待人工二筛`。
8. 所有可见普通文件都会进入处理；暂不支持正文提取的格式也会写入表格，并标记为 `待人工二筛`。

## 简历文件名格式

推荐把简历文件名改成这个格式：

```text
【岗位名称_期望工作地 薪资】候选人姓名 工龄.pdf
```

例子：

```text
【财务总监_北京 18-28K】郭燕婷 10年以上.pdf
```

也支持下划线格式：

```text
岗位名称_期望工作地_薪资_候选人姓名_工龄.pdf
```

例子：

```text
后端开发工程师_北京_12-18K_张松_3年.pdf
后端开发实习岗_北京_500-1000元_天_kanosa_一年以内.pdf
```

工具会从文件名里识别：

- 岗位名称
- 期望工作地
- 薪资
- 候选人姓名
- 工龄

如果文件名不符合这两种格式，工具仍然会尝试读取简历正文，但结果会进入 `待人工二筛`，因为岗位、地点、薪资、姓名、工龄无法稳定确认。

## 岗位名称和 sheet 对不上怎么办

工具会用文件名里的岗位名称去匹配岗位说明书里的 sheet。

例如文件名是：

```text
后端开发工程师_北京_12-18K_张松_3年.pdf
```

岗位说明书里需要有一个叫 `后端开发工程师` 的 sheet。

如果你的岗位说明书 sheet 叫 `全栈`，但简历文件名写的是 `后端开发工程师`，可以在 `config.yaml` 增加别名：

```yaml
job_aliases:
  后端开发工程师: "全栈"
  后端开发实习岗: "全栈"
```

网页版第一步也有“岗位别名映射”，一行写一个：

```text
后端开发工程师=全栈
后端开发实习岗=全栈
```

如果没有对应 sheet，也没有别名，工具不能知道应该按哪个岗位要求筛选，会写成 `待人工二筛`。

## 第一次使用

打开终端，执行：

```bash
cd /Users/mac/Desktop/codex/.worktrees/resume-screening-cli
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install ".[test]"
```

如果已经执行过这些命令，以后不用重复执行。

## 配置 API

配置文件是：

```text
/Users/mac/Desktop/codex/.worktrees/resume-screening-cli/config.yaml
```

如果没有这个文件，先复制一份：

```bash
cp config.example.yaml config.yaml
```

打开 `config.yaml`，找到 `model` 这一段：

```yaml
model:
  provider: "openai-compatible"
  base_url: ""
  api_key: ""
  model: ""
  timeout_seconds: 60
  temperature: 0.1
  allow_without_model: true
```

改成你的 API 信息：

```yaml
model:
  provider: "openai-compatible"
  base_url: "https://你的API地址/v1"
  api_key: "你的API_KEY"
  model: "你的模型名"
  timeout_seconds: 60
  temperature: 0.1
  allow_without_model: false
```

说明：

- `base_url`：API 地址，通常以 `/v1` 结尾。
- `api_key`：你的 API Key。
- `model`：模型名称。
- `allow_without_model: false`：表示必须调用模型进行筛选。

如果 API 地址、API Key、模型名都配置完整，工具会优先调用模型进行筛选。即使保留 `allow_without_model: true`，它也只是在模型配置缺失时作为兜底。

如果暂时没有 API，也可以保留：

```yaml
allow_without_model: true
```

这样工具会运行，但需要模型判断的简历会进入 `待人工二筛`。

## 运行工具

### 方法一：网页版运行

双击项目目录里的：

```text
启动简历筛选网页版.command
```

第一次启动时脚本会：

1. 创建或复用 `.venv`
2. 安装当前工具
3. 如果没有 `config.yaml`，从 `config.example.yaml` 复制一份
4. 如果 React 页面还没有构建，自动运行 `npm install && npm run build`
5. 打开 `http://127.0.0.1:8765`

页面分四步：

1. 文件路径：确认简历文件夹、岗位说明书、招聘结果表
2. 模型配置：填写 API 地址、API Key、模型名、超时时间
3. 运行前预检：检查文件数量、岗位 sheet、岗位匹配、结果表可写、模型配置
4. 开始筛选：查看实时进度、日志和统计

如果模型超时或报错，工具会把对应简历写成 `待人工二筛`，并在日志里显示原因。

### 方法二：命令行运行

每次运行前，先进入目录并激活环境：

```bash
cd /Users/mac/Desktop/codex/.worktrees/resume-screening-cli
source .venv/bin/activate
```

然后运行：

```bash
resume-screening run --config config.yaml
```

运行结束后，终端会显示统计结果，例如：

```text
处理完成
处理数量：10
推荐初试：3
可考虑：2
待人工二筛：4
未通过：1
疑似重复：1
模型失败：0
提取失败/警告：1
文件名不规范：2
```

## 到哪里看结果

结果会写入这个表：

```text
/Users/mac/Downloads/小A科技（北京）组织招聘.xlsx
```

写入位置：

```text
多维表格
```

工具会在原表后面新增三列：

- `初筛分类`
- `初筛理由`
- `缺失信息`

不会删除或覆盖旧数据。

## 初筛分类是什么意思

工具会给每份简历分成四类：

- `推荐初试`：比较符合岗位要求，建议进入初试。
- `可考虑`：有一定匹配度，但没有特别确定。
- `待人工二筛`：信息缺失、岗位要求不完整、文件名不规范、模型失败等，需要人工看一眼。
- `未通过`：明显不符合岗位要求。

评分是 10 分制：

- 8 分及以上：通过
- 9 分及以上：优秀

## 常见问题

### 1. 提示模型超时

Excel 的 `缺失信息` 里会写：

```text
模型评估失败：请求超时
```

可以把 `config.yaml` 里的超时时间调大：

```yaml
timeout_seconds: 120
```

### 2. 提示 API 认证错误

检查：

- `api_key` 是否填错
- `base_url` 是否正确
- `model` 名称是否正确

### 3. 很多简历进入待人工二筛

常见原因：

- 文件名不符合支持的格式
- 文件名里的岗位名称匹配不到岗位说明书 sheet
- 简历是扫描 PDF，文本无法直接提取
- 没有配置模型 API

### 4. 重复简历为什么还会写入

这是故意设计的。

因为同一份简历可能改名后投递不同岗位，工具不会跳过它，而是新增一行并标红，方便你人工判断。

### 5. 会不会覆盖原来的招聘表

不会。

工具只会向下新增，不会覆盖历史行。

## 日常使用步骤

最简单的流程是：

1. 把简历放到 `/Users/mac/Downloads`，可以直接放在子文件夹里。
2. 确认岗位说明书已更新：
   `/Users/mac/Downloads/小A自动化岗位说明书.xlsx`
3. 确认 `config.yaml` 里的 API 已配置。
4. 运行：

```bash
cd /Users/mac/Desktop/codex/.worktrees/resume-screening-cli
source .venv/bin/activate
resume-screening run --config config.yaml
```

5. 打开结果表查看：
   `/Users/mac/Downloads/小A科技（北京）组织招聘.xlsx`
