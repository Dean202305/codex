# 小A简历筛选工具使用说明

这份文档给日常使用看，不讲代码。按下面步骤做，就可以在本机完成简历识别、模型初筛、结果写入 Excel。

## 一、这个工具能做什么

工具会自动完成这些事情：

1. 扫描你指定文件夹里的简历文件，包含子文件夹。
2. 读取岗位要求表，每次筛选前都会重新读取最新内容。
3. 提取简历正文，支持 PDF、JPG、PNG、DOC/DOCX、PPT/PPTX 等常见格式。
4. 调用模型进行岗位匹配分析、评分和初筛分类。
5. 把结果追加写入招聘结果 Excel，不覆盖历史记录。
6. 如果发现附件内容重复，会照常新增一行，并把该行标红。
7. 如果模型超时、API 报错、正文提取失败、岗位无法匹配，会标记为 `待人工二筛` 并写明原因。

初筛分类固定为四类：

- `推荐初试`
- `可考虑`
- `待人工二筛`
- `未通过`

评分为 10 分制：

- 7 分及以上：通过
- 9 分及以上：优秀

## 二、Windows 安装版怎么用

Windows 打包后会有两个文件：

```text
dist/小A简历筛选-windows-x64-setup.exe
dist/小A简历筛选-windows-x64.zip
```

日常推荐使用安装版：

1. 双击 `小A简历筛选-windows-x64-setup.exe`。
2. 安装器会检测 Microsoft Edge WebView2 Runtime。
3. 如果缺少 WebView2，保持安装器勾选项，确认后会自动下载安装。
4. 安装完成后启动 `小A简历筛选`。
5. 第一次打开会先进入 `模型配置`，本地大模型缺失时需要点击 `下载并安装` 并确认下载。

说明：

- 安装版不需要安装 Python、Node.js 或命令行环境。
- 本地大模型文件不会塞进安装包，首次使用由用户确认后下载到当前电脑。
- 配置会保存在 Windows 当前用户目录：

```text
%APPDATA%\小A简历筛选\config.yaml
```

- 第一次打开时 Windows Defender 可能会扫描较久，等一下即可。

如果不想安装，也可以使用 `小A简历筛选-windows-x64.zip`：

1. 右键解压，不要直接在压缩包里双击运行。
2. 打开解压后的 `小A简历筛选` 文件夹。
3. 双击 `小A简历筛选.exe`。

## 三、macOS 本地版怎么用

这次生成的 macOS 版本有三个文件：

```text
dist/小A简历筛选.app
dist/小A简历筛选-mac.zip
dist/小A简历筛选-mac.dmg
```

日常推荐使用 dmg：

1. 双击 `小A简历筛选-mac.dmg`。
2. 把 `小A简历筛选.app` 拖到 `Applications`。
3. 从“应用程序”里打开 `小A简历筛选`。

如果要复制到另一台 Mac，也可以复制 zip 包，先解压：

```text
dist/小A简历筛选-mac.zip
```

第一次打开后，如果 macOS 提示来自未认证开发者，可以右键 App，选择“打开”。当前这台电脑生成的是 Apple Silicon 版，适合 M 系列芯片 Mac；Intel Mac 需要单独打包对应版本。

macOS 配置会保存在：

```text
~/Library/Application Support/小A简历筛选/config.yaml
```

注意：macOS 包需要内置当前架构对应的本地模型运行器。如果打包时提示缺少 `llama-server`，需要先按 `packaging/runtime/README.md` 放好运行器，再重新打包。

## 四、准备文件

### 1. 简历文件夹

默认简历文件夹是：

```text
/Users/mac/Downloads
```

可以在 App 里改成其他路径。工具会递归扫描这个文件夹，也就是子文件夹里的文件也会处理。

建议只放需要筛选的简历或分析文档，避免无关文件也被写入结果表。

### 2. 岗位要求表

默认岗位要求表是：

```text
/Users/mac/Downloads/小A自动化岗位说明书_副本.xlsx
```

每次开始筛选前都会重新读取这个表，所以岗位要求更新后不需要重启软件。

支持两种结构：

- 一个 sheet 对应一个岗位。
- 一个 sheet 里用表格维护多个岗位，一行一个岗位，包含 `岗位名称`、`岗位别名`、`核心职责`、`岗位jd` 等字段。

### 3. 招聘结果表

默认招聘结果表是：

```text
/Users/mac/Downloads/小A科技（北京）组织招聘.xlsx
```

工具只会向下新增，不会覆盖历史内容。

结果会新增必要列，例如：

- `初筛分类`
- `初筛理由`
- `缺失信息`

## 五、简历文件名建议

强烈建议按下面格式命名简历：

```text
【岗位名称_期望工作地 薪资】候选人姓名 工龄.pdf
```

例子：

```text
【财务总监_北京 18-28K】郭燕婷 10年以上.pdf
```

也支持下划线格式：

```text
岗位名称_期望工作地_薪资_候选人姓名_工龄.pdf
```

例子：

```text
后端开发工程师_北京_12-18K_张松_3年.pdf
```

工具会从文件名里识别：

- 岗位名称
- 候选人期望工作地
- 岗位标注薪资
- 候选人姓名
- 工龄

如果文件名不规范，工具仍会尝试识别正文，但通常会标记为 `待人工二筛`，因为关键信息无法稳定确认。

## 六、模型配置

打开 App 后，先进入“模型配置”。

目前有两种模式：`本地模型` 和 `自定义模型`。

### 方式 A：本地模型

适合不想配置外部 API，或希望软件在新电脑上尽量独立运行的场景。

使用步骤：

1. 在“模型配置”选择 `本地模型`。
2. 查看 `安装环境检测`，确认运行器、模型文件、应用数据目录状态。
3. 如果提示模型缺失，点击 `下载并安装`。
4. 弹窗会显示模型名称、大小和保存位置，确认后开始联网下载。
5. 下载完成后点击 `检测可用`。
6. 显示“本地模型服务可用”后，再运行预检和筛选。

注意：

- 本地模型权重不会直接放进安装包，需要首次使用时确认下载。
- 当前默认模型约 5.3GB，下载需要稳定网络和足够磁盘空间。
- 本地模型速度取决于电脑性能，老电脑可能推理较慢。
- 下载过程中可以取消；取消后不会安装未完成文件。

### 方式 B：自定义模型

适合使用阿里云百炼、OpenAI 兼容接口、公司内部模型服务、LM Studio、Ollama 等。

需要填写：

- `API 地址`：通常以 `/v1` 结尾。
- `API Key`：模型服务的密钥。
- `模型名`：服务端可识别的模型名称。
- `超时时间`：建议先用 120 秒，简历较长时更稳。

示例：

```text
API 地址：https://你的模型服务地址/v1
API Key：你的 API Key
模型名：你的模型名
```

建议勾选 `自定义模型不可用时自动使用本地模型`。这样预检会先检测自定义模型，如果 API 地址、Key、模型名或网络不可用，会自动切换到本地 Qwen 模型继续分析，避免大量结果直接进入 `待人工二筛`。

## 七、App 内筛选流程

### 第 1 步：模型配置

选择 `本地模型` 或 `自定义模型`，配置完成后点击 `保存配置`。

如果选择本地模型，需要先下载并检测可用。

如果选择自定义模型，建议保留 `自定义模型不可用时自动使用本地模型` 为勾选状态。

### 第 2 步：文件路径

填写或确认：

- 简历文件夹
- 岗位说明书
- 招聘结果表
- 岗位别名映射

岗位别名用于解决“简历文件名里的岗位”和“岗位要求表里的岗位名称”不完全一致的问题。

写法是一行一个：

```text
后端开发工程师=全栈
后端开发实习岗=全栈
```

填完后点击 `保存配置` 或 `运行预检`。

### 第 3 步：运行前预检

点击 `运行预检`。

预检会检查：

- 简历文件夹是否存在。
- 岗位说明书是否存在。
- 招聘结果表是否存在并可写入。
- 待处理文件数量。
- 模型配置是否可用。
- 本地模型是否缺少模型文件或运行器。

如果预检失败，先按页面提示修正，再重新预检。

### 第 4 步：开始筛选

点击 `开始筛选`。

筛选过程中可以看到：

- 当前处理文件。
- 进度条。
- 各分类统计。
- 实时日志。

如果需要中断，点击 `停止筛选`。已写入 Excel 的结果会保留，未处理的文件不会继续处理。

## 八、筛选完成后看哪里

打开招聘结果表：

```text
/Users/mac/Downloads/小A科技（北京）组织招聘.xlsx
```

重点看这些列：

- `初筛分类`：推荐初试、可考虑、待人工二筛、未通过。
- `初筛理由`：模型给出的详细理由，包括匹配点和风险点。
- `缺失信息`：文件名缺失项、正文提取问题、模型错误、重复提醒等。

如果整行标红，表示工具检测到该附件内容和历史处理过的附件内容一致，需要人工确认是否重复投递。

## 九、结果分类怎么理解

`推荐初试`：

候选人与岗位关键词画像匹配度较高，综合评分达到 7 分及以上，建议进入初试。

`可考虑`：

有部分匹配点，但还有不确定项，适合人工再看一眼。

`待人工二筛`：

通常不是直接淘汰，而是信息不足或系统无法稳定判断。常见原因包括文件名不规范、岗位无法匹配、正文提取失败、模型超时、API 报错等。

`未通过`：

明显不符合岗位核心要求，或关键条件差距较大。

## 十、换电脑使用

### Windows 换电脑

优先复制安装包到新电脑：

```text
小A简历筛选-windows-x64-setup.exe
```

在新电脑双击安装后启动软件。

如果使用免安装版，也可以复制：

```text
小A简历筛选-windows-x64.zip
```

解压后双击 `小A简历筛选.exe`。

第一次使用需要重新确认：

- 简历文件夹路径。
- 岗位说明书路径。
- 招聘结果表路径。
- 模型配置。

如果使用本地模型，新电脑第一次也需要下载模型文件。

### macOS 换电脑

复制：

```text
小A简历筛选-mac.zip
```

解压后双击 App。第一次使用同样需要重新确认文件路径和模型配置。

## 十一、常见问题

### 1. 为什么全部都是待人工二筛

优先检查：

- 是否真的配置了可用模型。
- API Key 是否正确。
- API 地址是否以 `/v1` 结尾。
- 模型名是否写对。
- 本地模型是否已经下载并检测可用。
- 文件名里的岗位是否能匹配到岗位说明书。

如果没有配置模型，或者勾选了模型失败兜底，工具会把很多结果写成 `待人工二筛`。

### 2. 提示模型超时

把超时时间调大，例如 120 或 180 秒。

如果是本地模型，说明当前电脑推理较慢，可以减少同时处理的文件量，或改用自定义 API 模型。

### 3. 图片或扫描 PDF 识别不出来

图片和扫描 PDF 依赖 OCR。当前默认 OCR 命令是：

```text
tesseract
```

如果电脑没有安装 OCR，普通 PDF、DOCX、PPTX 仍可处理，但图片和扫描 PDF 可能进入 `待人工二筛`。

### 4. 招聘结果表打不开或写入失败

先关闭 Excel 里的结果表，再重新运行筛选。

如果 Excel 正在打开并锁定文件，工具可能无法写入。

### 5. 文件名重复怎么办

文件名重复不代表内容重复。工具会按内容 hash 判断附件是否处理过。

如果内容重复，会新增一行并标红，不会自动跳过。

### 6. 本地模型提示运行器不存在

说明当前 App 包不完整，缺少：

```text
llama-server.exe
```

或 macOS 对应的：

```text
llama-server
```

Windows 请使用已经通过校验的最新版安装包或 zip。macOS 需要重新放置运行器并打包。

## 十二、开发版启动方式

如果不用打包 App，也可以在项目目录启动网页版。

项目目录：

```text
/Users/mac/Desktop/codex/.worktrees/resume-screening-cli
```

双击：

```text
启动简历筛选网页版.command
```

或命令行运行：

```bash
cd /Users/mac/Desktop/codex/.worktrees/resume-screening-cli
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install ".[test,desktop]"
resume-screening web --config config.yaml --host 127.0.0.1 --port 8765
```

打开：

```text
http://127.0.0.1:8765
```

## 十三、命令行筛选

如果只想用命令行：

```bash
cd /Users/mac/Desktop/codex/.worktrees/resume-screening-cli
source .venv/bin/activate
resume-screening run --config config.yaml
```

运行结束后会输出统计，例如：

```text
处理完成
处理数量：10
推荐初试：3
可考虑：2
待人工二筛：4
未通过：1
疑似重复：1
模型失败：0
提取失败/警告：1
文件名不规范：2
```

日常优先使用本地 App；命令行主要用于调试或批量自动化。
