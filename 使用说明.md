# 简历初筛工具使用说明

这份文档给日常使用看，不讲代码，只讲怎么配置、怎么运行、结果怎么看。

## 这个工具做什么

这个工具会自动做这几件事：

1. 扫描 `/Users/mac/Downloads` 里面的简历文件。
2. 每次运行都重新读取岗位要求表：
   `/Users/mac/Downloads/小A自动化岗位说明书.xlsx`
3. 识别简历内容，并根据岗位要求调用模型做初步筛选。
4. 把结果追加写入招聘表：
   `/Users/mac/Downloads/小A科技（北京）组织招聘.xlsx`
5. 不覆盖历史记录，只往下新增。
6. 如果发现简历内容重复，会照常新增一行，但整行标红。
7. 如果模型超时、API 报错、文件名不规范、岗位要求不完整，会标记为 `待人工二筛`。

## 简历文件名格式

推荐把简历文件名改成这个格式：

```text
【岗位名称_期望工作地 薪资】候选人姓名 工龄.pdf
```

例子：

```text
【财务总监_北京 18-28K】郭燕婷 10年以上.pdf
```

工具会从文件名里识别：

- 岗位名称
- 期望工作地
- 薪资
- 候选人姓名
- 工龄

如果文件名不符合格式，工具仍然会尝试读取简历正文，但结果会默认进入 `待人工二筛`。

## 第一次使用

打开终端，执行：

```bash
cd /Users/mac/Desktop/codex/.worktrees/resume-screening-cli
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install -e ".[test]"
```

如果已经执行过这些命令，以后不用重复执行。

## 配置 API

配置文件是：

```text
/Users/mac/Desktop/codex/.worktrees/resume-screening-cli/config.yaml
```

如果没有这个文件，先复制一份：

```bash
cp config.example.yaml config.yaml
```

打开 `config.yaml`，找到 `model` 这一段：

```yaml
model:
  provider: "openai-compatible"
  base_url: ""
  api_key: ""
  model: ""
  timeout_seconds: 60
  temperature: 0.1
  allow_without_model: true
```

改成你的 API 信息：

```yaml
model:
  provider: "openai-compatible"
  base_url: "https://你的API地址/v1"
  api_key: "你的API_KEY"
  model: "你的模型名"
  timeout_seconds: 60
  temperature: 0.1
  allow_without_model: false
```

说明：

- `base_url`：API 地址，通常以 `/v1` 结尾。
- `api_key`：你的 API Key。
- `model`：模型名称。
- `allow_without_model: false`：表示必须调用模型进行筛选。

如果暂时没有 API，也可以保留：

```yaml
allow_without_model: true
```

这样工具会运行，但需要模型判断的简历会进入 `待人工二筛`。

## 运行工具

每次运行前，先进入目录并激活环境：

```bash
cd /Users/mac/Desktop/codex/.worktrees/resume-screening-cli
source .venv/bin/activate
```

然后运行：

```bash
resume-screening run --config config.yaml
```

运行结束后，终端会显示统计结果，例如：

```text
处理完成
处理数量：10
推荐初试：3
可考虑：2
待人工二筛：4
未通过：1
疑似重复：1
模型失败：0
提取失败/警告：1
文件名不规范：2
```

## 到哪里看结果

结果会写入这个表：

```text
/Users/mac/Downloads/小A科技（北京）组织招聘.xlsx
```

写入位置：

```text
多维表格
```

工具会在原表后面新增三列：

- `初筛分类`
- `初筛理由`
- `缺失信息`

不会删除或覆盖旧数据。

## 初筛分类是什么意思

工具会给每份简历分成四类：

- `推荐初试`：比较符合岗位要求，建议进入初试。
- `可考虑`：有一定匹配度，但没有特别确定。
- `待人工二筛`：信息缺失、岗位要求不完整、文件名不规范、模型失败等，需要人工看一眼。
- `未通过`：明显不符合岗位要求。

评分是 10 分制：

- 8 分及以上：通过
- 9 分及以上：优秀

## 常见问题

### 1. 提示模型超时

Excel 的 `缺失信息` 里会写：

```text
模型评估失败：请求超时
```

可以把 `config.yaml` 里的超时时间调大：

```yaml
timeout_seconds: 120
```

### 2. 提示 API 认证错误

检查：

- `api_key` 是否填错
- `base_url` 是否正确
- `model` 名称是否正确

### 3. 很多简历进入待人工二筛

常见原因：

- 文件名不符合格式
- 岗位说明书对应 sheet 内容不完整
- 简历是扫描 PDF，文本无法直接提取
- 没有配置模型 API

### 4. 重复简历为什么还会写入

这是故意设计的。

因为同一份简历可能改名后投递不同岗位，工具不会跳过它，而是新增一行并标红，方便你人工判断。

### 5. 会不会覆盖原来的招聘表

不会。

工具只会向下新增，不会覆盖历史行。

## 日常使用步骤

最简单的流程是：

1. 把简历放到 `/Users/mac/Downloads`。
2. 确认岗位说明书已更新：
   `/Users/mac/Downloads/小A自动化岗位说明书.xlsx`
3. 确认 `config.yaml` 里的 API 已配置。
4. 运行：

```bash
cd /Users/mac/Desktop/codex/.worktrees/resume-screening-cli
source .venv/bin/activate
resume-screening run --config config.yaml
```

5. 打开结果表查看：
   `/Users/mac/Downloads/小A科技（北京）组织招聘.xlsx`
